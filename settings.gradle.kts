pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "kmpizza"
include(":androidApp")
include(":shared")
include(":backend")
